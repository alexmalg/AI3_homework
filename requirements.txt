# ============================================================================
# requirements.txt - Список зависимостей проекта
# ============================================================================
# Этот файл содержит все Python пакеты, необходимые для работы приложения.
# Для установки всех зависимостей используйте команду:
#     pip install -r requirements.txt
#
# Формат: имя_пакета==версия
# Точная версия обеспечивает стабильность и предсказуемость приложения
# ============================================================================

# Flask - микрофреймворк для создания веб-приложений
# Используется для создания HTTP сервера и обработки маршрутов
Flask==3.0.0

# requests - библиотека для выполнения HTTP запросов
# Используется для отправки POST запросов к API MentorPiece
requests==2.31.0

# python-dotenv - загрузка переменных окружения из .env файла
# Используется для безопасного хранения API ключей
python-dotenv==1.0.0

# Werkzeug - WSGI утилиты (автоматически устанавливается с Flask)
# Нужен для работы Flask
Werkzeug==3.0.1

# ============================================================================
# ТЕСТИРОВАНИЕ (для разработки)
# ============================================================================

# pytest - фреймворк для написания и запуска тестов
# Используется для юнит-тестирования приложения
pytest==7.4.3

# pytest-mock - расширение для pytest для облегчения работы с mock объектами
# Позволяет легко мокировать внешние зависимости (HTTP запросы, API и т.д.)
pytest-mock==3.12.0
